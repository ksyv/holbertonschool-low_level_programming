#include "main.h"
#include <stdio.h>
/**
 * main - print the program's name, followed by a new line
 * @argc: number of argument
 * @argv: argument
 * Return: 0
 */
int main(__attribute__((unused))int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
